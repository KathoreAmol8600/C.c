#include<stdio.h>
void main()
{
    char str[20];
    gets(str);
     int count=0;
    for(int i=0;str[i]!='\0';i++)
    {    
              count++;
    }
    printf("vovels %d constatb is %d",vovel,con);
}